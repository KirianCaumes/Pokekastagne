import React from 'react'
import { AppProps } from 'app'// eslint-disable-line

/**
 * Select lobby
 * @param {AppProps} props
 */
export default function IndexGame({ test }) {
    return (
        <p>IndexGame</p>
    )
}